# TONOTE_피드백_엣지케이스분석.md

[평가 방법]: 예상치 못한 사용자 행동 엣지케이스 분석 (10가지 시나리오)  
[평가 날짜]: 2026-06-05  
[서비스명]: TONOTE

---

| 번호 | 피드백 내용 | 심각도 | 반영 여부 | 반영 내용 또는 반영 못 한 이유 |
|------|------------|--------|----------|-------------------------------|
| 1 | AI 탭에 "치킨 먹고 싶다", "ㅋㅋㅋ" 등 향수 무관 텍스트 입력 시 KW 딕셔너리 매칭 실패로 엉뚱한 향수가 추천됨 | 중 | 부분 반영 | 매칭 결과 없을 때 "조금 더 구체적인 감정이나 장면을 말씀해 주시면..." 안내 메시지 노출. 향수 무관 입력 감지 후 역질문 버블은 미구현 |
| 2 | 취향 진단 6단계 진행 중 전화·탭 전환 등으로 이탈 시 진행 상태가 메모리에서 사라져 처음부터 재시작해야 함 | 상 | 미반영 | 진단 단계 및 선택값을 sessionStorage/localStorage에 임시 저장하는 기능 미구현. 재진입 시 복귀 팝업도 없음 |
| 3 | 샘플 신청 폼 작성 중 브라우저 뒤로가기 버튼 클릭 시 입력한 배송 정보가 모두 초기화됨 | 중 | 미반영 | SPA 구조상 브라우저 history API 미연결. 입력값 임시 저장 및 복원 로직 미구현 |
| 4 | 지하철·해외 로밍 등 네트워크 불안정 환경에서 Leaflet 지도 CDN 로딩 실패 시 회색 빈 박스만 표시되고 원인 안내 없음 | 상 | 부분 반영 | try-catch로 오류를 잡아 콘솔 로그 처리. 매장 목록은 지도와 독립적으로 렌더링됨. "지도를 불러올 수 없어요" 안내 UI 및 재시도 버튼은 미구현 |
| 5 | 헤비 유저가 좋아요를 100개 이상 누를 경우 '좋아요 모아보기' 모달 스크롤이 매우 길어지고 원하는 향수 찾기 어려움 | 하 | 미반영 | 모달 내 검색·필터·페이지네이션 미구현. 마이 탭 가로 스크롤 항목 수 제한도 없음 |
| 6 | "따뜻하고 아늑한 느낌" 입력 시 KW 딕셔너리 매핑과 향수 mood 점수 계산 과정에서 기대와 다른 계열 향수가 추천될 수 있음 | 상 | 부분 반영 | KW 딕셔너리 확장(침대·수면·안락 등) 및 컨텍스트 분기 로직으로 일부 개선. 결과 피드백(👍/👎) 버튼 및 재추천 기능은 미구현 |
| 7 | 커뮤니티 글 등록 버튼을 빠르게 두 번 탭하거나 응답 지연으로 여러 번 누를 시 같은 글이 중복 등록될 수 있음 | 중 | 미반영 | submitPost() 실행 직후 버튼 즉시 disabled 처리 및 스피너 표시 미구현. 동일 텍스트 중복 등록 감지 로직 없음 |
| 8 | 장기 사용자의 localStorage 용량 초과 시 리뷰·커뮤니티·샘플 데이터 저장 실패가 조용히 발생하고 사용자에게 알리지 않음 | 중 | 부분 반영 | 모든 저장 로직에 try-catch 적용됨. catch 블록에서 사용자용 에러 토스트 미노출. 용량 현황 표시 UI 미구현 |
| 9 | 처음 사용하는 유저가 비교 기능 진입 경로(상세 페이지 '비교 추가' 버튼)를 몰라 마이 탭 비교 목록만 눌렀을 때 쇼케이스 예시가 나와 혼란 | 중 | 부분 반영 | 쇼케이스 모드에 "✦ 예시 비교" 안내 뱃지 및 "탐색 탭에서 향수를 담아보세요" 안내 추가. 단계별 사용법 가이드는 미구현 |
| 10 | 가족 등 여러 사람이 같은 기기를 공유할 경우 단일 localStorage로 인해 리뷰·좋아요·취향 진단 결과·커뮤니티 댓글이 모두 뒤섞임 | 상 | 미반영 | 계정 개념 없는 로컬 스토리지 단일 구조. "이 데이터는 이 기기에만 저장됩니다" 안내 문구는 설정창에 있으나, 멀티 프로필 전환 및 클라우드 동기화는 구현 범위 외 (UCD 수업 프로토타입 한계) |

---

[한 줄 요약]:  
**진단 중 이탈·네트워크 오류·다인 기기 공유·AI 오추천 총 4가지의 '상' 심각도 케이스가 모두 사용자가 서비스에서 이탈하는 결정적 순간과 직결되며, 이 중 세션 상태 임시 저장과 오류 피드백 UI 두 가지가 가장 적은 개발 비용으로 가장 큰 경험 개선 효과를 낼 수 있는 우선 과제다.**
